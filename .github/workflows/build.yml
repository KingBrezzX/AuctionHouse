name: Build AuctionHouse

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Build
        run: mvn -B -U clean verify

      - name: Upload JAR
        uses: actions/upload-artifact@v4
        with:
          name: AuctionHouse-Java25-Paper26.2
          path: target/AuctionHouse-1.0.0.jar
          if-no-files-found: error

